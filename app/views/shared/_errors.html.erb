<% if obj.errors.any? %>
  <div class='row'>
    <div class='alert alert-danger span8'>
      <h5>Vennligst korriger følgende feil</h5>
      <ul>
        <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>